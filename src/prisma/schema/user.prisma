model User {
  id       String @id @default(cuid())
  email    String @unique
  username String
  todos    Todo[]

  createdAt DateTime  @default(now())
  updatedAt DateTime  @default(now()) @updatedAt
  deletedAt DateTime?

  @@map("user")
}
